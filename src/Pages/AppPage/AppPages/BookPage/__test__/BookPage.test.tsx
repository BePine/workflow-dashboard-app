import React from 'react';
import { render, screen } from '@testing-library/react';
import BookPage from '../BookPage';

describe("BookPage",()=>{
    test('renders BookPage element', () => {
        // render(<BookPage/>);
        // const bookPageElement = screen.getByTestId('bookPageTest');
        // expect(bookPageElement).toBeVisible();
      });
})
