import React from 'react';
import { render, screen } from '@testing-library/react';
import TaskPage from '../TaskPage';

describe("TaskPage",()=>{
    test('renders TaskPage element', () => {
        // render(<TaskPage/>);
        // const taskPageElement = screen.getByTestId('taskPageTest');
        // expect(taskPageElement).toBeVisible();
      });
})
